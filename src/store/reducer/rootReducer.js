const initState = {
    user: [
        { id: 1, name: 'Chen'},
        { id: 2, name: 'Jun'},
        { id: 3, name: 'Ju'}
    ]
}

const rootReducer = ( state = initState, action ) {
    
    return state;
}

export default rootReducer;